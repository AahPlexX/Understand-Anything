# Secrets — never commit
.env.openrouter
.env*.local

# Claude Code local overrides — machine-specific, never commit
.claude/settings.local.json

# VS Code PERSONAL overrides — machine-specific, never commit
# .vscode/settings.json is intentionally committed (shared proxy integration config)
# Only create .vscode/settings.local.json for personal machine overrides
.vscode/settings.local.json

# Proxy runtime logs
/tmp/ua-proxy.log

# Node
node_modules/
*.log

# OS
.DS_Store
Thumbs.db
