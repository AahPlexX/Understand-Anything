# Secrets — never commit
.env.openrouter
.env*.local

# Claude Code local overrides — machine-specific
.claude/settings.local.json

# VS Code local settings — contain env overrides specific to this machine
.vscode/settings.json

# Proxy runtime logs
/tmp/ua-proxy.log

# Node
node_modules/
*.log

# OS
.DS_Store
Thumbs.db
