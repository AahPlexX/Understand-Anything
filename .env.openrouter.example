# .env.openrouter.example
# Copy this to .env.openrouter and fill in your real key.
# .env.openrouter is gitignored and will NEVER be committed.
#
# Get a free OpenRouter API key at: https://openrouter.ai/keys
# Free models require no billing setup — just sign up.

OPENROUTER_API_KEY="sk-or-v1-REPLACE_WITH_YOUR_KEY"
PROXY_PORT=4099
